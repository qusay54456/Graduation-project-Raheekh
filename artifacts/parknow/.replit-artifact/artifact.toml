kind = "web"
previewPath = "/"
title = "ParkNow"
version = "1.0.0"
id = "artifacts/parknow"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19334

[services.development]
run = "pnpm --filter @workspace/parknow run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/parknow", "run", "build" ]
publicDir = "artifacts/parknow/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19334"
BASE_PATH = "/"
